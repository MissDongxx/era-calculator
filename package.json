{
  "name": "era-calculator-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx wrangler pages dev . --compatibility-date=2026-06-18",
    "deploy": "npx wrangler pages deploy . --project-name=era-calculator",
    "check": "node tools/check-site.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
